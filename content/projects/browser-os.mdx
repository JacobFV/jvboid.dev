---
title: browser-os
date: 2025-11-28
resume_description: "generic browser-native desktop shell with window manager, virtual filesystem, and app lifecycle that the windows-web-next and macos-web-next simulators inherit from. typescript, svelte, vercel."
lane: building
tags: [agents, ui, sim]
summary: "Browser-native desktop shell — generic OS chrome that windows-web-next and macos-web-next inherit from."
icon:
  src: "/assets/img/projects/browser-os-favicon.svg"
  alt: "browser-os desktop shell favicon"
video: "https://www.youtube.com/watch?v=JVPbi7MRZFg"
links:
  github: https://github.com/JacobFV/browser-os
  demo: https://desktop-shell.vercel.app
influences: []
realizes: []
critiques: []
---

## Problem
The windows-web-next and macos-web-next simulations share a lot of plumbing: window manager, virtual filesystem, app lifecycle, taskbar/menu chrome. Re-implementing each per-OS is wasteful and drifts the two apart.

## Solution
A generic browser-native desktop shell that the OS-specific simulators reuse. Same window management, same app surface, same agent affordances — just different chrome and default apps.

## How
- **Stack:** TypeScript / Svelte, deployed on Vercel.
- **Deployed at:** [desktop-shell.vercel.app](https://desktop-shell.vercel.app).

## Results
Foundation layer for the controllable-desktop family. Both windows-web-next and macos-web-next pull from this shell.

<Video
  url="https://www.youtube.com/watch?v=JVPbi7MRZFg"
  title="browser-os"
  caption="The shell running: window management, virtual filesystem, and app lifecycle before any OS-specific chrome is layered on."
/>

## Lessons
Splitting the chrome from the shell makes new "OS" simulators cheap. Worth doing earlier in similar product-research stacks.

## Used by
[`synthux`](/projects/synthux) drives this desktop natively for visual computer-use dataset capture. The synthux executor installs a small launcher overlay, clicks the launcher button for a target surface via a real low-level mouse event, and the click flows through the existing `taskbar:shortcut:clicked` event so an app opens identically to a real taskbar click. The SynthUX bridge in `packages/os/src/synthuxBridge.ts` exposes `window.__synthuxLaunchApp(appId)` for that path.
